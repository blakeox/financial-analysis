# Archived UI components

These components are **not used in production** and are **not exported** from `packages/ui/src/index.ts`.

| File                  | Production replacement                                                   |
| --------------------- | ------------------------------------------------------------------------ |
| `Navbar.tsx`          | `apps/web/src/components/ModernNavBar.astro`                             |
| `VSCodeChatPanel.tsx` | `apps/web/src/components/ChatPanel.astro`                                |
| `ChatPanel.tsx`       | `apps/web/src/components/ChatPanel.astro` (Wave 5 — removed from barrel) |

Kept for reference only. See `packages/ui/SLIM.md` for the deferred feature-dashboard move list.
