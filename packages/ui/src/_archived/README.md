# Archived UI components

These components are **not used in production**. The web app uses Astro equivalents instead.

| File                  | Production replacement                       |
| --------------------- | -------------------------------------------- |
| `Navbar.tsx`          | `apps/web/src/components/ModernNavBar.astro` |
| `VSCodeChatPanel.tsx` | `apps/web/src/components/ChatPanel.astro`    |

Kept for reference and historical tests. Do not export from `packages/ui/src/index.ts`.
